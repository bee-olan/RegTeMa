{% extends 'base.html.twig' %}

{% block title %}Members{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Главная</a></li>
        <li class="breadcrumb-item"><a href="{{ path('work') }}">Работа</a></li>
        <li class="breadcrumb-item"><a href="{{ path('work.members') }}">Участники</a></li>
        <li class="breadcrumb-item active">{{ member.name.full }}</li>
    </ol>
{% endblock %}

{% block body %}

    {{ include('app/work/members/_tabs.html.twig') }}

    <div class="controls">
        <a href="{{ path('work.members.edit', {'id': member.id}) }}" class="btn btn-primary">Редактировать</a>
        <a href="{{ path('work.members.move', {'id': member.id}) }}" class="btn btn-primary">Перевести в другую
            группу</a>
        <a href="{{ path('users.show', {'id': member.id}) }}" class="btn btn-primary">Смотреть Пользователей</a>
        {% if member.active and member.id.value != app.user.id %}
            <form action="{{ path('work.members.archive', {'id': member.id}) }}" method="post" onsubmit="return
            confirm('Вы   уверены ?')">
                <input type="hidden" name="token" value="{{ csrf_token('archive') }}" />
                <button type="submit" class="btn btn-danger">Архивировать</button>
            </form>
        {% endif %}
        {% if member.archived %}
            <form action="{{ path('work.members.reinstate', {'id': member.id}) }}" method="post" onsubmit="return confirm('Вы   уверены?')">
                <input type="hidden" name="token" value="{{ csrf_token('reinstate') }}" />
                <button type="submit" class="btn btn-success">Восстановить</button>
            </form>
        {% endif %}
    </div>

    <div class="box">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Ф.И. или Ник</th>
                    <th>Email</th>
                    <th>Group</th>
                    <th>Status</th>
                </tr>
                <tr>
                    <td>{{ member.name.full }}</td>
                    <td>{{ member.email.value }}</td>
                    <td>{{ member.group.name }}</td>
                    <td>{{ member_status(member.status.name) }}</td>
                </tr>
            </tbody>
        </table>
    </div>

        {% if departments %}
        <div class="box">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Проекты</th>
                        <th>Отделы</th>
                    </tr>
                </thead>
                <tbody>
                    {% for department in departments %}
                        <tr>
                            <td><a href="{{ path('work.projects.project.show', {'id': department.project_id}) }}">{{ department.project_name }}</a></td>
                            <td>{{ department.department_name }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}


{% endblock %}