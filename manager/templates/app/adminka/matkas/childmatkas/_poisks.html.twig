{% set menu = knp_menu_get('adminka_matkas_childmatka_poisks', [], {
    'plemmatka_id': plemmatka ? plemmatka.id : null,
    'route_params': app.request.attributes.get('_route_params')|merge(app.request.query.all)
} ) %}
{{ knp_menu_render(menu, {'currentClass': 'active'}) }}