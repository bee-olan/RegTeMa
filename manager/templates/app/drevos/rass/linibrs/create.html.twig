{% extends 'base.html.twig' %}

{% block title %}Создать{% endblock %}


{#{% block breadcrumbs %}#}
{#    <ol class="breadcrumb">#}
{#        <li class="breadcrumb-item"><a href="{{ path('home') }}">Главная</a></li>#}
{#        <li class="breadcrumb-item"><a href="{{ path('adminka.rasas') }}">Раса -- {{ rasa.title }}</a></li>#}
{#        <li class="breadcrumb-item active">Добавить другую лини</li>#}
{#    </ol>#}
{#{% endblock %}#}

{% block body %}

<article class="rasa">
<h3 	class="rasa__title  ">Раса : <strong  class="stro--kras">  {{ rasa.name }}</strong>  -  {{ rasa.title }}</h3>
	<div class="linia ">

<h4 class="rasa__title"><strong class="stro--kras">Линия </strong> - определяемся с названием линии
     для БД</h4>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btnn btnn--mb0 btnn--pt">Добавить в БД</button>
            <a href="{{ path('drevos.rass.linibrs',{'id': rasa.id}) }}"
                class="btnn btnn--mb0 btnn--pt"> Выйти из режима "Добавить"</a>
        {{ form_end(form) }}
	</div>
</article>

{% endblock %}