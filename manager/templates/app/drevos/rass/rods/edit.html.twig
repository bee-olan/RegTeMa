{% extends 'base.html.twig' %}

{% block title %}{{ rasa.name }}{% endblock %}

    {% block kroxii %}
		<a href="{{ path('home') }}">Главная</a>
		<a href="{{ path('drevos.rass') }}">Родословная</a>
		<a href="{{ path('drevos.rass', {'id':rasa.id} ) }}">Раса - {{ rasa.title }}</a>
		<a href="{{ path('drevos.rass.rods', {'id':rodo.id} ) }}">Раса - {{ rasa.title }}</a>
		<span class="aktiv ">Редактировать</span>
	{% endblock %}

{% block body %}

	{{ include('app/drevos/_tabs.html.twig') }}

	<div class="box">

		<b class="dre_title ">Родословная <strongo class="strongo--krox">  Редактировать</strongo></b>
	</div>
	<div class="box-smotrim">

	<div class="box-edit">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btnn btnn--mb0 btnn--pt">Добавить измения в БД</button>
			<a href="{{ path('drevos.rass.rods',{'id': rasa.id}) }}"
		   		class="btnn btnn--mb0 btnn--pt"> Выйти из режима "Добавить"
			</a>
            {{ form_end(form) }}
	</div>
</div>

{% endblock %}