{% extends 'base.html.twig' %}

{% block title %}{{ rasa.name }}{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Главная</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka') }}">Пасека</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas') }}">Раса</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas.rasa.show', {'id': rasa.id}) }}">{{ rasa.name }}</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas.rasa.settings', {'rasa_id': rasa.id}) }}">Settings</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas.rasa.settings.pchelowods', {'rasa_id': rasa.id}) }}">Пчеловоды</a></li>
        <li class="breadcrumb-item active">Назначать</li>
    </ol>
{% endblock %}

{% block body %}

  {% include 'app/paseka/rasas/rasa/_tabs-r.html.twig' %}

   {% include 'app/paseka/rasas/rasa/settings/_tabs-s.html.twig' %} 

    <div class="card">
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div>
                <button type="submit" class="btn btn-primary">Assign</button>
            </div>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}