{% extends 'base.html.twig' %}

{% block title %}Roles{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Главная</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka') }}">Работа</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas') }}">Проекты</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas.kategors') }}">Роли</a></li>
        <li class="breadcrumb-item"><a href="{{ path('paseka.rasas.kategors.show', {'id': kategor.id}) }}">{{ kategor.name }}</a></li>
        <li class="breadcrumb-item active">Copy</li>
    </ol>
{% endblock %}

{% block body %}

     {{ include('app/paseka/_tabs-p.html.twig') }}
    
    <div class="card">
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Save</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}