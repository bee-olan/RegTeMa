{% extends 'base.html.twig' %}

{% block title %}Группы{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Главная</a></li>
        <li class="breadcrumb-item"><a href="{{ path('matkis') }}">Матки</a></li>
        <li class="breadcrumb-item active">Группы</li>
    </ol>
{% endblock %}

{% block body %}

    <div class="controls">
        <a href="{{ path('matkis.u4astniks.groups.create') }}" class="btn btn-success">Добавить группу</a>
    </div>

    <div class="box">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Название</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% set token = csrf_token('delete') %}
                {% for group in groups %}
                    <tr>
                        <td>{{ group.name }}</td>
                        <td>
                            <div class="d-flex flex-row">
                                <a href="{{ path('matkis.u4astniks.groups.edit', {'id': group.id}) }}" class="btn btn-sm btn-primary mr-1"><span class="fa fa-pencil"></span></a>
                                <form action="{{ path('matkis.u4astniks.groups.delete', {'id': group.id}) }}" method="post" onsubmit="return confirm('Вы уверены?')">
                                    <input type="hidden" name="token" value="{{ token }}" />
                                    <button type="submit" class="btn btn-sm btn-danger"><span class="fa fa-trash"></span></button>
                                </form>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}