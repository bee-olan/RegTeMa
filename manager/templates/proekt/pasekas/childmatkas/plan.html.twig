{% extends 'proekt/base.html.twig' %}

{% block title %}{{ childmatka.name }}{% endblock %}



{% block bodyy %}
    <div  class="proekts proekts--childmatka">
    <div class="inklud inklud--child">
        <div class="inklud__img  ">
            <img class="" src="{{ asset('assets/proekt/images/matkas/p4ela1.jpg') }}" alt="">
        </div>

        <div class="inklud__title  teni teni--kategor">
            Редактировать ком-рий  ДочьМатки  {{childmatka.name}}
        </div>

    </div>

{#    <h1 class="mt-0 mb-3 font-xl">#}
{#        {% if childmatka.parent %}#}
{#            <a href="{{ path('work.projects.childmatkas.show', {'id': childmatka.parent.id}) }}">{{ childmatka.parent.name }}</a> /#}
{#        {% endif %}#}
{#        {{ childmatka.name }}#}
{#    </h1>#}

    <div class="plem-form__header  ">
        <div class="child-form ">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div>
                <button type="submit" class="btnn  btnn--pt btnn--col-child">
                Установить новую дату</button>
            </div>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}